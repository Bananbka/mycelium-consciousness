FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml uv.lock ./
COPY shared ./shared
COPY services ./services

RUN pip install --no-cache-dir uv && uv sync --package api --frozen

EXPOSE 8000

CMD ["uv", "run", "api"]
