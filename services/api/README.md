# Admin API

FastAPI service for administrative operations over clone profiles and memory
metadata.

The initial Lab 1 endpoint is `GET /health`, which verifies that the service can
open an asynchronous PostgreSQL session and execute a simple query.
