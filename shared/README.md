# Shared Package

Shared database and schema code used by the services.

It contains the SQLAlchemy async engine/session factory, ORM models, pgvector
fields, protobuf contracts, and Alembic migration environment.
